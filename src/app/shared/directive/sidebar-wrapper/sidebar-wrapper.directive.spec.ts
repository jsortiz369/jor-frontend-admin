import { SidebarWrapperDirective } from './sidebar-wrapper.directive';

describe('SidebarWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
